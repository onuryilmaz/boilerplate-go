box:
  id: golang:1.5.0
build:
  steps:
    - script:
        name: go test
        code: |
          go test -v ./...
    - script:
        name: go build 
        code: |
          go build github.com/onuryilmaz/boilerplate-go/cmd
