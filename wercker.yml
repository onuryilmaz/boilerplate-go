box:
  id: google/golang
build:
  steps:
    - script:
        name: go test
        code: |
          go test -v ./...
    - script:
        name: go build 
        code: |
          go build github.com/onuryilmaz/boilerplate-go/cmd
